{{>layout/header}}
{{#userName}} <!-- mustache에는 if문이 없어 항상 최종값을 넘겨줘야 한다. 따라서 userName만 넘겨 존재하면 이름과 로그아웃 버튼을 표시한다-->
<h1>스프링부트로 시작하는 웹 서비스</h1>
<a href="/ranking"></a>
<a href="/profile"></a>
<div class="col-md-12">
    <div class="row">
        <div class="col-md-6">
            <a href="/posts/save" role="button" class="btn btn-primary">글 등록</a>
            Logged in as: <span id="user">{{userName}}</span>
            <a href="/logout" class="btn btn-info active" role="button">Logout</a>
            <a href="/ranking" role="button" class="btn btn-primary">랭킹</a>
            <a href="/profile" role="button" class="btn btn-primary">프로필</a>
        </div>
    </div>
    <br>
    <!-- 목록 출력 영역 -->
    <table class="table table-horizontal table-bordered">
        <thead class="thead-strong">
        <tr>
            <th>게시글번호</th>
            <th>제목</th>
            <th>작성자</th>
            <th>최종수정일</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#posts}}
            <tr>
                <td>{{id}}</td>
                <td><a href="/posts/update/{{id}}">{{title}}</a></td>
                <td>{{author}}</td>
                <td>{{modifiedDate}}</td>
            </tr>
        {{/posts}}
        </tbody>
    </table>
</div>
{{/userName}}
{{^userName}} <!-- userName이 존재하지 않는 경우에는 로그인 버튼을 표시한다. -->
<div>
    <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
</div>
{{/userName}}
{{>layout/footer}}